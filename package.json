{
  "name": "ucsd-tennis-court-watcher",
  "version": "0.7.2",
  "private": true,
  "description": "Personal UCSD Recreation tennis court availability watcher Chrome extension",
  "scripts": {
    "test": "node --test",
    "check": "node --check background.js && node --check content.js && node --check login.js && node --check popup.js && node --check lib/booking-core.js"
  }
}
